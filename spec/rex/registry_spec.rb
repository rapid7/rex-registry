require 'spec_helper'

describe Rex::Registry do
  it 'has a version number' do
    expect(Rex::Registry::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
